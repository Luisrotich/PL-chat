steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/private-chat:latest', '.']
    
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/private-chat:latest']

options:
  logging: CLOUD_LOGGING_ONLY

timeout: 1200s